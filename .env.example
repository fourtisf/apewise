# Public site URL — used for canonical/OG/sitemap absolute URLs.
NEXT_PUBLIC_SITE_URL=https://apewise.ai

# TODO: real waitlist capture. When set, /api/waitlist will POST submissions here
# (e.g. a webhook to Sheets/Notion/CRM). Left blank, the route just validates + logs.
WAITLIST_WEBHOOK_URL=
